specVersion: "v1beta"
serviceId: "swallow-hero"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "swallow-hero-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
